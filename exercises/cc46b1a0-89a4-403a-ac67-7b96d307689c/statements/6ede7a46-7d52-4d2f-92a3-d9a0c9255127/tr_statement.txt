<p>Bir tablo oluşturduktan sonra, bazı alan veya kısıtlama eklemeyi unuttuğumuzu, belki eklediğimiz bazı kısıtlamaların artık gerekli olmadığını veya belki bazılarının adını değiştirmek istediğimizi fark etmemiz mümkündür. alanlar. Mümkün mü? Şimdi öyle olduğunu göreceğiz ve hemen hemen her durumda <a href="https://www.postgresql.org/docs/14/sql-altertable.html" target="_blank"><code komutunu kullanacağız. >TABLO DEĞİŞTİR< /code></a>.</p><h2>Tabloları yeniden adlandırın</h2><p>Bir tablonun adını değiştirmek istiyorsak <code>RENAME</code> kullanırız. <code>ALTER TABLE komutu </code> yan tümcesi.</p><h3>Sözdizimi:</h3><blockquote><pre><code>ALTER TABLE [ VARSA ] TabloAdı<br /> Yeni_adı Yeniden Adlandır ;</code></pre> </blockquote><h3>Örnek:</h3><p>Önceki bir alıştırmada aşağıdaki ifadeyle <code>USERS</code> tablosunu oluşturduk:</p> <blockquote><pre><code> TABLO KULLANICILARI OLUŞTUR (<br /> FIRST_NAME VARCHAR(25),<br /> LAST_NAME VARCHAR(30)<br />);</code></pre></blockquote> <p>Tabloyu < code>STUDENTS</code> olarak yeniden adlandırmak için aşağıdaki komutu yürütmeliyiz:</p><blockquote><p><code>ALTER TABLE <strong>USERS</strong> RENAME TO < Strong>ÖĞRENCİLER</strong>;</ code></p></blockquote><h3>Alıştırma:</h3><p>Aşağıdakileri oluşturduğumuzu hayal edin Sonraki tablo:</p><blockquote><pre><code>TABLO KONULARI OLUŞTUR (<br /> Ad VARCHAR(100),<br /> NumHours INTEGER<br />);</code></pre> </blockquote><p><code>SUBJECTS</code> tablosunu <code>MODULES</code> olarak yeniden adlandırın.</p>