<h2>Kısıtlama <a href="https://www.postgresql.org/docs/14/ddl-constraints.html#DDL-CONSTRAINTS-PRIMARY-KEYS" target="_blank"><code>PRIMARY KEY</ code></a>.</h2><p>İlişkisel modelde tabloların bir birincil anahtarı olmalıdır. Tabloyu oluşturduğumuzda, hangi sütunların bu birincil anahtarın parçası olacağını belirtmemiz gerekeceği açıktır.</p><p>Birincil anahtar, onu oluşturan alanları <code>NULL DEĞİL</code> yapar ve alanların değerleri <code>UNIQUE</code> türündedir.</p><p>Birincil anahtar <code>Login< olsaydı <code>USERS</code> tablosunun nasıl görüneceğini görelim. /code> alanı :</p><blockquote><pre><code>TABLO KULLANICILARI OLUŞTUR (VARCHAR (25) BİRİNCİL ANAHTAR Oturum Aç);</code></pre></blockquote><p>Veya ad kısıtlaması :</p><blockquote><pre><code>TABLO KULLANICILARI OLUŞTUR (<br /> VARCHAR'a Giriş (25)<br /> CONSTRAINT Users_log_PK BİRİNCİL ANAHTAR<br />);</code></code></ pre>< /blockquote><h3>Alıştırma:</h3><p>Karşılık gelen kısıtlamayı ekleyerek, önceki alıştırmadaki tablonun birincil anahtarı olarak <code>ID</code> niteliğini tanımlar.</p>