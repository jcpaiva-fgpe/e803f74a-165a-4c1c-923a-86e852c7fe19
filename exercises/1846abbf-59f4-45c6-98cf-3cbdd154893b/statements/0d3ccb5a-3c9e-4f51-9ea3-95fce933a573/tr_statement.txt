<h2><code>DROP TABLE</code></h2><p>Bir tablo artık kullanışlı olmadığında ve ona ihtiyacımız olmadığında, onu silmek uygundur; bu şekilde yer kaplamaz ve adını başka bir durumda kullanabiliriz.</p><p>Bir tabloyu silmek için <a href="https://www.postgresql.org/" komutunu kullanacağız. docs/14/sql- droptable.html" target="_blank"><code>DROP TABLE</code></a>.</p><h3>Söz dizimi:</h3><blockquote><pre>< code>DROP TABLE TabloAdı [ CASCADE];</code></pre></blockquote><p>Bu ifade, verileri (satırları) dahil olmak üzere veritabanı tablosunu siler. Veri Sözlüğünde o tabloda bulunan tüm bilgiler de silinecektir.</p><h3>Örnek:</h3><p>Komutla bir <code>USERS</code> tablosu oluşturduysak : <blockquote><pre><code>TABLO KULLANICILARI OLUŞTUR (<br /> Giriş VARCHAR (25)<br /> KISITLAMA Users_log_PK BİRİNCİL ANAHTAR<br />);</code></pre></blockquote>< p >Şu komutla kaldırabiliriz:</p><blockquote><pre><code>DROP TABLE USERS;</code></pre></blockquote><p>Bu komutu kullanırken dikkatli olun, bir tablonun silinmesi geri alınamaz ve yürütülmeden önce herhangi bir onay istemi yoktur.</p><h3>Alıştırma:</h3><p>Şimdi <code>Users</code> ve <code oluşturduğumuzu hayal edin. > tablo Modülleri</code> aşağıdaki kodla birlikte:</p><blockquote><pre><code>CREATE TABLE USERS (<br /> Giriş VARCHAR (25)<br /> CONSTRAINT Users_log_PK PRIMARY KEY<br /> ); <br /><br />TABLO MODÜLLERİ OLUŞTUR (<br /> name VARCHAR(100) PRİMARY KEY,<br /> hours INTEGER<br />);</code></pre></blockquote> <p>Nasıl <code>MODULES</code> tablosunu siler misiniz?</p>