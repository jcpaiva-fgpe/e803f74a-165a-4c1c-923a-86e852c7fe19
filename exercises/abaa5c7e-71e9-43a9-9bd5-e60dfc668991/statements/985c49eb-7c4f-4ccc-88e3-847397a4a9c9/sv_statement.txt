<h2>Klausul <code>ON DELETE</code>.</h2><p>Klausulen <code>ON DELETE</code> låter dig bestämma hur databasen upprätthåller referensintegritet när du tar bort en primär eller unik nyckel. Om vi ​​inte använder <code>ON DELETE</code>-satsen tillåter inte databashanteraren att en nyckel som refereras till i <em>förälder</em>-tabellen tas bort om den har någon rad i <em> tabell. dotter</em> som refererar till det.</p><h3>Övning:</h3><p>Ändra lösningen från föregående övning så att <code>MODULER< när en post raderas från tabellen /code > alla poster i BOOKS-tabellen som är relaterade till den raderade posten raderas automatiskt.</p>