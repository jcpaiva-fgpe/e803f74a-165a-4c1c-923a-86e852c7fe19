<p><code>SQL</code>'in verileri depolamak için çalıştığı temel nesneler, zaten bildiğimiz gibi bir dizi <em>satır</em> 
ve kesişimi <em>hücre</em> olarak adlandırılan <em>sütunlar</em>. Bu, bilgi öğelerinin, toplamak istediğimiz verilerin depolanacağı yerdir.</p><p>Tabloyu oluşturmadan önce, bazı ayrıntıları planlamak uygundur:</p><ul><li>< Strong>Tabloya</strong> hangi ad vereceğiz</strong>.</li><li><strong>Her bir <strong>masaya</strong> hangi adı vereceğiz? >sütunlar</strong >.</li><li><strong>Her sütunda ne tür ve boyutta veri depolayacağız?</li><li><strong>Hangi kısıtlamalar</strong>? güçlü> elimizde var mı? </li><li>İhtiyacımız olan diğer <strong>ek bilgiler</strong>.</li></ul><p>Ve diğer < <strong>tablo adları için</strong> karşılanması gereken güçlü>kurallar</strong>:</p><ul><li>Aynı şemada (kullanıcı) yinelenen tablo adlarına sahip olamayız.</li> <li>Alfabetik bir karakterle başlamalıdırlar.</li><li>Maksimum uzunluk 30 karakterdir.</li><li>Yalnızca İngilizce harflere, rakamlara veya alt çizgiye izin verilir.</li><li> Yapamazsın <code>SQL</code> ayrılmış kelimeleri eşleştirmek için (örneğin, bir <code>WHERE</code> tablosunu çağıramayız).</li><li>Büyük/küçük harf duyarlı değildir. </li><li >Adın boşlukları veya ulusal karakterleri olması durumunda (yalnızca bazı veritabanlarında izin verilir), genellikle çift tırnak içine alınır.</li></ul><h3> Sözdizimi</h3><p>Temel sözdizimi tablo oluşturmak için kullanılan komut aşağıdaki gibidir:</p><blockquote><pre><code>CREATE TABLE [şema.]tablo adı (sütun1 veri türü, sütun2 veri türü, ..sütunN veri türü );</code> </pre></blockquote><p>burada:</p><ul><li><code>sütun1, sütun2, ..., sütunN</code> tablonun içereceği sütunların adlarıdır .</li><li><code>veri türü</code>, her sütunun veri türünü belirtir.</li></ul><h3>Örnek:</h3 ><p>< adlı bir tablo oluşturmak için <code>VARCHAR</code> türünde iki alana sahip güçlü>ÖĞRENCİLER</strong>:</p><blockquote><pre><code>TABLO ÖĞRENCİLERİ OLUŞTUR (<br /> ad V) ARCHAR(25),<br /> soyadı VARCHAR(50)<br />);</code></pre></blockquote><h3>Alıştırma:</h3><p>Bir tablo oluşturun <code> MODÜLLER</code>, aşağıdaki özelliklerden oluşur:</p><ul><li><code>ad</code>: <code>VARCHAR</code> türünde ve 100 boyutunda.</li>< li><code>hours</code>: türü <code>INTEGER</code>.</li></ul><p><small><strong>Not:</strong> Nasıl yapılacağını daha sonra göreceğiz. Tabloları silmek için Ancak, tabloları doğru veya daha hassas bir şekilde yeniden yapabilmek için mutlaka silmeniz gerekeceğinden, <code>DROP TABLE table_name;</code> komutunu kullanmanız gerekir. Örneğin: <ul><li><code>DROP TABLE KULLANICILARI;</code>,</li><li><code>DROP TABLE MODÜLLERİ;</code> veya </li><li><code> DROP TABLO ÖĞRENCİLERİ;</code></small></li></ul></p>