CREATE TABLE MODULES (
    CODE INTEGER,
    NAME VARCHAR(100),
    DEGREE CHAR(3),
    PRIMARY KEY (CODE, DEGREE)
);

CREATE TABLE BOOKS (
    ISBN NUMERIC PRIMARY KEY,
    TITLE VARCHAR(100),
    MODULE INTEGER,
    DEGREE CHAR(3),
    FOREIGN KEY (MODULE, DEGREE) REFERENCES MODULES (CODE, DEGREE)
);