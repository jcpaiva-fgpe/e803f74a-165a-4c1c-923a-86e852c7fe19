ALTER TABLE MODULES
    ADD PRIMARY KEY (CODE, DEGREE);
    
ALTER TABLE BOOKS
    ADD PRIMARY KEY (ISBN);
    
ALTER TABLE BOOKS
    ADD FOREIGN KEY (MODULE, DEGREE) REFERENCES MODULES (CODE, DEGREE);